import { create } from "@storybook/theming";

export default create({
  title: "One Storybook",
  base: "light",

  // Typography
  fontBase: '"Open Sans", sans-serif',
  fontCode: "monospace",

  // UI
  // appBg: "#f6f7f8",
  // appContentBg: "#fdfdfd",
});
